-c requirements.txt
# テスト専用の依存（ランタイムには含めない）。
pytest>=8.0
httpx>=0.27
pytest-cov>=5.0
# 静的解析（CI の lint ジョブ用。ランタイムには含めない）。設定は backend/pyproject.toml。
ruff>=0.16.2
# mypy は初回導入なので 1.x 系に固定（2.x への major bump は Dependabot 経由で
# レビュー付き別 PR に回す。config はこの系列で緑を確認済み）。
mypy>=2.3.0,<3
