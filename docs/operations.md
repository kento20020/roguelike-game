# 運用・検証・未決事項（Operations）
> 元 `game_design_document.md`（v1.2）の §18・§21 を再配置。相互参照の §番号・OPEN-xxx は元GDD準拠。運用インフラ（Alembic/Docker/バックアップ/構造化ログ/監視/CI）は OPEN-027 として未整備＝runbook.md 骨子＋各OPEN参照。

## 18. バランス検証フレームワーク

### 18.1 合格条件

| 指標 | 目標（効果量ベース） | 状態 |
|------|------|:---:|
| **強プレイのクリア率** | **25〜40%**（bot強化状態を固定して測定） | ★確定 |
| ランダムプレイのクリア率 | 1〜5% | 暫定 |
| スキル幅（強−ランダム差） | クリア率差 ≥ X pt（CI下限が0を跨がない＋最小効果量） | 併走 |
| 初手別クリア率差 | ±5pt以内（最大−最小の効果量で判定） | 暫定 |
| 選択感度 | 効果量 ≥ Y pt（p値のみで判定しない） | 併走 |
| 死亡フロア分布 | 終盤集中は許容。最終フロア以外の単一フロアが死亡の Z% を超えない | 暫定 |
| 単一mod寄与 | ±8pt以内（平均寄与のみ） | 暫定 |
| modペア寄与（synergy） | 主要ペアの交互作用が ±W pt 以内 | 暫定（追加・OPEN-023） |
| 初クリアまでの期待ラン数（人間相当bot） | 観測のみ・閾値なし（win-to-progress の体験曲線監視・OPEN-029） | 暫定（追加） |

> **有意性ハック回避（§18.4 Goodhart）**：bot大量試行では n が大きく僅差でも p<0.05・CI0非跨ぎを満たすため、スキル幅・選択感度・初手差は**最小効果量（X/Y pt）を CI下限とともに要求**する（`balance_stats.py` の TOST=同値検定も併用）。
> **死亡フロア分布**：難度ランプのあるパーマデスでは終盤集中が正常。「集中しない」は偽陽性を生むため、終盤集中を許容し「最終フロア以外の単一フロアが過度に偏らない」基準へ置換。
> **クリア率帯の測定条件**：帯（25-40%）は bot の `permanent_upgrades_state` に依存する（+attack等は全戦闘を短縮）。**未強化パネルを基準**とし、フル強化は別帯で管理（bot 強化状態を固定して測る）。
> **strong戦略の版固定**：クリア率帯は strong bot の賢さに対する相対値。`strategy_version` で strong 戦略を凍結し「確定帯は当該版に対してのみ有効」とする（戦略変更時は帯を再検証）。**バランス検証全体が bot の人間代表性に依存する**点は運用上の前提として明記。
> **効果量閾値の事前登録**：X/Y/Z/W の値は**Phase2 開始前に数値で確定して本表へ記入する（事前登録）**。データを見てから閾値を決めるのは効果量版 p-hacking であり禁止。値の決定はデザイナー（人間）、以後の変更は変更履歴で管理。
> **確定帯と guard の不整合（OPEN-018）**：帯 25-40% は **guard 無し bot** での測定値だった。v1.8 でジャストガード再設計・bots.py（strong-v2）への受け方策実装・感度分析による数値確定（heavy0.9/decay0.5）が完了し、guard 数値起因の Phase1 ブロッカーは解消した。**Phase1 前に残るのは深度カーブ・depth_scaling 等の数値調整**（§18.2 の v1.4注記）。

### 18.2 検証フェーズ

| フェーズ | 目的 | ツール |
|---------|------|------|
| Phase 0 | 実装完了・全テストgreen（既知P0=0） | pytest / TS unit / `loader.validate` |
| Phase 1 | クリア率帯域 | phase12_harness.py |
| Phase 2 | スキル幅・選択感度 | + balance_stats.py |
| Phase 3 | mod単体・ペア影響（FDR） | balance_stats.py(e-BH) + balance_analysis.py(ablation) |
| Phase 4 | 経済・sink ROI・hoarder | balance_analysis.py |
| Phase 5 | 回帰テスト（CI） | GitHub Actions |

> **注記**：Phase0「バグゼロ」は検証不能のため「全テストgreen／既知P0=0」に置換。`fun_metrics.py` は特定Phaseに紐づかない**探索的ツール（合格条件外）**。Phase3 の e-BH は `balance_stats.py`、mod アブレーションは `balance_analysis.py`（旧記述は balance_analysis 単独に見えたため訂正）。guard（§8.4）を正式化する場合は bot 戦略に guard を含めて Phase1-4 を再測定（OPEN-018）。Phase5 の GitHub Actions は **docs-ci に加えて balance.yml（pytest＋バランス回帰 `test_balance_regression`）が稼働済み**（旧記述「未整備」は古い）。回帰基準の再生成は `python -m app.simulation.gen_baseline`（意図的変更の承認手順）。
> **v1.4 の基準値変化（要デザイナーレビュー）**：フロア可変深度化（§4.1）と heal 1ターン消費（OPEN-020）により、maxed 強bot のクリア数は 18/120 → **6/120（5%）** に低下した（ベースライン再生成済み）。確定帯 25〜40% は未強化パネル基準のため直接比較ではないが、**深度カーブ・depth_scaling・回復経済の数値調整（デザイナー）を Phase1 前に行う**こと。調整ノブ: floors.json `depth`・config `depth_scaling`・heal コスト・OPEN-018 guard 係数。
> **v1.8 の基準値変化**：ジャストガード再設計＋strong-v2（受け方策）により maxed クリア率は 5% → **10.2%（N=1000・ベースライン 15/120）** へ回復。base/mid は依然 0% で帯 25〜40% には未達＝**上記の数値調整（デザイナー）は引き続き Phase1 前に必要**。

### 18.3 ツール群

> `backend/app/simulation/` の実在ファイルに同期（v1.1）。

| ツール | 機能 | 状態 |
|--------|------|------|
| phase12_harness.py | Bot大量試行（強/random・CRN対応） | 実在 |
| bots.py | botプレイヤー（戦略実装） | 実在 |
| balance_stats.py | e-value統一（BernoulliCS・e-BH・McNemar・TOST・ハザード・レーシング） | 実在 |
| balance_analysis.py | sink ROIアブレーション・hoarder検出 | 実在 |
| balance_report.py | レポート生成 | 実在 |
| fun_metrics.py | 体験指標（面白さ系メトリクス） | 実在 |
| gen_baseline.py | ベースライン生成 | 実在 |
| guard_sensitivity.py | guard係数の感度分析（政策×係数グリッド） | 実在 |
| INTEGRATION.md | 統計手法とフェーズのマッピング | 実在 |
| llm_content_pipeline.py | Generator→Validator→Simulator→Critic | **将来・未実装**（INTEGRATION.md に記載） |

### 18.4 Goodhart回避
- 統計指標は合格条件であり最適化目標ではない
- 死にmodは潰す・壊れmodは残す（非対称運用）
- 帯域内の判断はデザイナーの感性で決める

### 18.5 受け入れ基準（完了の定義 / Definition of Done）

§18.1 はバランスの合格条件。本節は**実装完了の定義**を分けて明文化する（v1.1で追加）。

**機能**
- 新規ラン開始 → 1F〜5F進行 → 5Fゲート通過で `cleared`、HP0で `dead` に遷移する
- ノード選択・戦闘・宝箱（開封/リロール）・回復・ゲートが各phaseで解決できる
- クリア時に恒久強化ポイントを1獲得し割り振れる

**データ（§20.5 機械検証）**
- config/enemies/mods/floors のロード成功
- 全非カオス敵の behaviors weight 合計＝100／全フロアのゲート確率合計＝1.0（loader.validate・v1.4）
- アンロック整合（多親=親2／単親=親1・生成フロアは `validate_floor` が生成毎に検証）・存在しないノード参照なし

**API**
- 全エンドポイントが正常系/異常系テストを通過
- phase不整合=409／チップ不足・未対応操作=400／不明session=404（§25.4）

**UI**
- 各phaseに対応画面が表示される／`available_actions` 外の操作は実行不可
- ツリーは row1=画面下・ゲート=画面上で描画（§4・§26.1）

**RNG / テレメトリー**
- 同一seed・同一操作列で同一結果（Python/TS黄金テスト）
- ラン終了時に RunRecord が保存され、`cleared`/`dead` が正しく記録される

> **TS版SFC32の用途**：本番のクライアント計算は無い（原則1「Reactは計算しない」）。TS黄金テストは将来のオフライン/演出用途に向けた**RNGパリティの凍結**であり、演出で使う場合も rng 状態は露出しない演出専用シードに限定する。

---

## 21. 未決事項

> 解消済み項目の確定経緯は巻末「付録: 変更履歴」を参照。本章は現時点の残課題のみを扱う。
> 内部議論ラベル（問E/問G/問J）は管理ID `OPEN-xxx` に振り直した（旧ラベルは括弧で併記）。
> **受け入れ条件（done定義）**：各OPENの方針欄末尾に測定可能な受入条件を付す。データ駆動項目は Phase 判断に委ね、非データ項目（OPEN-011/013/015/018/024 等）は具体的 done を定義する。
> **v1.2 で追加**：OPEN-011〜027 は実装コードとの実差分監査（doc-code drift audit）で洗い出した項目。**v1.3 で OPEN-028/029 を追加**し、OPEN-004/017/018/020/024/025 を改訂（分割後の横断整合レビュー反映）。多くは「コードは実装済みだが GDD 記述が古い/欠落」または「GDD/JSON にもコードにも無い真の欠落」。**2026-07-11 の設計書ギャップ監査で OPEN-030〜042 を追加**（[reports/2026-07-11_gdd_gap_audit.md](reports/2026-07-11_gdd_gap_audit.md) 参照。既存 OPEN・構成監査 F-xx と重複しない未追跡分のみ）。

### 21.1 確定状態マトリクス

「何が確定で、何が調整中で、何が未決か」の索引。本文inlineの「叩き台/暫定」を集約する。

| 項目 | 状態 | 決定フェーズ／備考 |
|------|------|------------------|
| 戦闘係数（counter 1.0 / heavy 1.8 / ramp_base 5） | **確定** | config.json・実装一致 |
| ゲート確率テーブル（§7.4） | **確定** | floors.json・データは合計1.0（CI検査はOPEN-013） |
| ゲート特殊効果（+40G確定・追加効果未実装・§7.4） | 一部確定 | OPEN-011 |
| 受け（guard・§8.4） | **確定（v1.8）** | OPEN-018 解消。感度分析（heavy0.9/decay0.5が支配戦略消滅かつスキル表現最大）で数値確定（`docs/proposals/guard_redesign.md` §8） |
| メタ進行の永続（ProfileRow・§20.6） | **確定** | 実装一致 |
| 進行中状態の永続（アクションログ再生・§25.1） | **確定（v1.5）** | OPEN-007 解消。マルチユーザー認証は引き続き OPEN-026 |
| experience の正準キー（romaji enum・§20.7） | **確定（v1.4）** | OPEN-012 解消 |
| アンロック連鎖・生成規則（§4・可変深度＋格子ランタイム生成） | **確定（v1.4・規則として）** | 深度カーブ/depth_scaling の数値は叩き台（要デザイナーレビュー）・validate_floor で生成毎検証 |
| 強プレイのクリア率帯（25〜40%） | **確定（帯）・実測は帯未達** | §18.1。v1.4 深度化後 maxed 6/120＝Phase1 前に数値調整（§18.2 注記） |
| 敵HP/attack/gold_base（§11.3） | データ駆動調整中（叩き台） | Phase1〜 botで調整 |
| ゴールド期待値（§13.1） | データ駆動調整中（叩き台） | Phase4 |
| 恒久強化の効果値・上限 | データ駆動調整中（構造は確定） | OPEN-002 |
| sinkコスト（攻撃ブースト30G・回復係数） | データ駆動調整中 | OPEN-003 / Phase4 |
| 強敵判定の定義方式（暫定=絶対値4・終盤退化） | 未決 | OPEN-004 / Phase1前 |
| 固有名詞（敵・mod表示名） | 未決 | OPEN-001 |
| フェーズB（スタンス） | 非スコープ（将来） | OPEN-006 |

> §18.1 の暫定/併走指標（ランダムクリア率1-5%・スキル幅・初手別±5pt・選択感度・死亡フロア分布・単一mod寄与±8pt・modペア）は **§18.1 を正**とし、本マトリクスには集約しない（重複管理を避ける）。

### 21.2 未決事項（OPEN）

> **運用**: 実装着手時に GitHub Issue を「`OPEN-0xx: 題名`」で起票して状態列に `起票 #N`（着手後は `対応中 #N`）を記入し、実装PRに `Closes #N` を書く。解消時は解消版数を対応フェーズ列に記録し、changelog.md に経緯を残す（解消済みも行は残す既存慣行を踏襲）。手順の正本は [CONTRIBUTING.md](../CONTRIBUTING.md)。

| ID | 項目 | 内容 | 優先度 | 対応フェーズ | 方針 | 状態 |
|----|------|------|:------:|:----------:|------|:----:|
| OPEN-001 | 固有名詞確定 | 仮名→カジノテーマ名の最終確定（§11.1） | 中 | 実装後半 | §16.2の方向で全敵・全mod名を確定。v1.1では実置換しない | 未着手 |
| OPEN-002 | 恒久強化数値（旧問E） | 各強化項目の効果値・上限の最終調整＋**割り振り導線**（`/upgrade` のスコープ・バンク消化の許可phase・§12.3/§25.2） | 中 | Phase1後 | データ駆動。構造は確定済み。導線は `/api/profile/upgrade` 移設 or 許可phase明記 | 未着手 |
| OPEN-003 | sinkコスト（旧問G） | 攻撃ブースト30G・回復係数含む最終調整 | 低 | Phase4後 | ROI計測後 | 未着手 |
| OPEN-004 | 強敵判定の定義方式（旧問J） | 絶対閾値 diff≥4 は終盤退化（3F 4/8・4F **7/8**・5F **10/10** が強敵＝情報量ゼロ・§11.2）。UI表示・強敵報酬・宝箱確率・統計に波及 | 中 | Phase1前に暫定固定 | 「値」でなく**定義方式**（フロア相対・上位n体等）を再設計。暫定は絶対値4のまま。受入=定義方式の確定＋config反映 | 未着手 |
| OPEN-005 | 攻撃ブースト効果量 | +100%が適正か | 低 | Phase4 | データ駆動。叩き台+100% | 未着手 |
| OPEN-006 | フェーズB | スタンスシステム設計 | 低 | v1.x以降 | A案検証完了後（攻撃ブーストと計算経路共有） | 未着手 |
| OPEN-007 | セーブ/ロード・進行中状態の永続 | ブラウザ離脱＋**サーバ再起動での喪失**（`session_store` はメモリ・単一プロセス） | — | **解消（v1.5）** | GameStateの全フィールド（rng含む）をスナップショットする方式ではなく、**「seed＋初期upgrades＋適用アクション列」を`active_sessions`（SQLite）へ記録し、キャッシュミス時にゼロから再生する**方式を採用（同一seed→同一結果の不変条件をそのまま利用。Redis等の新規インフラは導入しない）。`session_store`はLRU上限（既定500）つきのホットキャッシュに変更、追い出されても`app.engine.replay.rebuild_engine`で透過的に復元される。受入=`store.clear()`（再起動相当）を挟んでも`GET /run/{id}`が中断前と同一状態を返す（`tests/test_session_persistence.py`）。同一`session_id`への複数ワーカー同時書き込みの競合ハードニングは対象外（既知の限界として残す） | 解消 |
| OPEN-008 | 演出・SE/BGM | オーディオ仕様 | 低 | 実装後期 | — | 未着手 |
| OPEN-009 | アクセシビリティ | 色覚対応・キーボード操作 | 低 | 実装後期 | — | 未着手 |
| OPEN-010 | 勝利優先時のHP | 同時死亡・勝利優先でHP0生存になる挙動 | — | **解消（v1.4）** | 勝利時 `hp = max(1, hp)` を実装・相打ちテストで担保（§8.2） | 解消 |
| OPEN-011 | ゲート特殊効果 | 特殊の追加効果（現状+40Gのみ・「次フロア強化等」未実装）（§7.4） | 低 | 実装後期 | 効果実装なら受入=効果定義＋発生率検証。当面は +40G 確定 | 未着手 |
| OPEN-012 | experience の enum 化 | 日本語→romaji 正準キー（grind/gamble/race/dodge/chaos）（§20.7） | — | **解消（v1.4）** | enemies.json・labels.ts・統計キーを一括変更。受入条件（日本語がAPI/統計キーに出ない）を loader.validate で強制 | 解消 |
| OPEN-013 | データCI検査の拡充 | gate合計1.0・kind整合・経路≥2・上限Lv=21・snapshot非出力（§20.5） | — | **解消（v1.4）** | loader.validate＋validate_floor（生成毎）＋テスト（snapshot非出力・上限Lv21）に分担して実装（§20.5） | 解消 |
| OPEN-014 | 体験タイプ最早出現の制約化 | 段階導入をCI検証（ずれ2F/カオス3F〜）（§4.4/§9.1） | 低 | Phase1後 | §9.1 制約をCIへ。受入=違反プールが fail | 未着手 |
| OPEN-015 | L3情報の state別マスク | 未インタラクトノードの name/max_hp 非送出（§5） | — | **解消（v1.4）** | `_node_view` を state別マスク化（L2常時・L3はインタラクト後）。受入条件をテストで担保 | 解消 |
| OPEN-016 | ゲート保証の削減0停止 | 大ダメ0%到達後の重ねがけ拒否（§7.4） | — | **解消（v1.4）** | 400＋action非提示を実装 | 解消 |
| OPEN-017 | 宝箱リロールの判断材料/廃止 | 中身非公開で期待値改善0（Phase4判断）。1F確定宝箱分は解消済み | 低 | Phase4 | 一般はヒント付与 or 廃止を継続検討。**1F確定宝箱（fixed_mod）のリロールは v1.4 で 400＋非提示に解消** | 一部解消 |
| OPEN-018 | guard の正式化・数値是正 | 旧数値（deal0.5/incoming0.25・無コスト・回数無制限）は非ランプ敵（ロスター約7割）に対し**総被ダメ半減の支配戦略**になり得た | 中 | **解消（v1.8）** | ジャストガード再設計＋bots.py strong-v2＋感度分析で数値確定（heavy0.9/decay0.5が支配戦略消滅かつスキル表現最大）。受入条件（bot行動空間=UI一致）達成（§8.4・`docs/proposals/guard_redesign.md` §8） | 解消 |
| OPEN-019 | -sinkコスト下限 | scout無料化（0G）防止（§13.2） | — | **解消（v1.4）** | `max(1,…)` クランプ実装。最安sink≥1G | 解消 |
| OPEN-020 | heal sink のターン消費 | battle中healで敵が反応するか（§13.2） | — | **解消（v1.4）** | 推奨の「1ターン消費」案を実装（heal後に敵ロール発火・§13.2）。guard 数値（OPEN-018）との複合再測定は Phase1 で実施 | 解消 |
| OPEN-021 | 攻撃ブースト異常系 | evade巻戻し丸損・pending中二重課金（§13.2/§25.4） | — | **解消（v1.4）** | evade時は boost 持越し＋pending中の再購入400 | 解消 |
| OPEN-022 | 反射チュートリアル確実化 | 1Fで反射の取得/発火を保証（§14.2） | 低 | 実装後半 | 必須経路配置 or A必須経由。受入=1F内で取得&発火 | 未着手 |
| OPEN-023 | modペア交互作用ゲート | synergy寄与の統計ゲート（§18.1） | 中 | Phase3 | `balance_analysis` アブレーションをペア拡張（e-BH） | 未着手 |
| OPEN-024 | RunRecord data_version | 版混在の統計汚染防止（§19.1） | — | **解消（v1.4）** | 4JSON sha256短縮ハッシュ＋strategy_version を Alembic `0002_telemetry` で追加（§19.1） | 解消 |
| OPEN-025 | sink use_count / gate_results / guard記録 | ROI/寄与の逆算用テレメトリ（§19.1） | — | **解消（v1.4）** | sink_use_counts・gate_results・action_counts（attack/guard/heal_*）を記録。全ランの操作履歴も run_actions へ永続化 | 解消 |
| OPEN-026 | 認証・冪等性・stats scope | 進行中DB永続・冪等キー・stats/history のスコープ（§25） | 中 | 実装後半 | 単一プレイヤーは非スコープ明記（§0.4）＋ stats scope＋全mutating POSTに冪等キー | 未着手 |
| OPEN-027 | 運用基盤の整備 | Alembic・Docker・game.dbバックアップ・構造化ログ+req_id・.env.example・CI・`/health`・RUNBOOK/ONBOARDING（§22/§23） | 低 | **大半解消（v1.4）** | 解消済み: Alembic・ログ＋X-Request-ID・.env.example・`/health`・CI（balance.yml/pytest）＋frontend-ci（lint/build）・runbook更新・CORS限定。残: Docker・バックアップ自動化 | 大半解消 |
| OPEN-028 | カオスの ramp_hit 空砲 | カオスは increment=0 のため ramp_hit 被ダメ常時5。ramp偏重（最大85%）を引いた個体はほぼ無害＝実効強度の分散が極端（§9.2） | 低 | Phase1後 | 「意図的な当たり枠」と明記 or カオス専用の小 increment 付与（データ変更のみ）。受入=§9.2 に評価を明文化 | 未着手 |
| OPEN-029 | 初クリアまでの体験曲線 | win-to-progress は初クリアまでメタ進行ゼロ（§2.1）。人間相当の弱めbotでの初クリア期待ラン数が長すぎると離脱点になる | 低 | Phase2 | §18.1 の観測指標で監視（閾値なし）。超過が見えた場合のみ救済（到達フロア別微小ポイント等）を検討 | 未着手 |
| OPEN-030 | Phase1前の数値調整 | 深度カーブ（floors.json `depth`）・`depth_scaling`・回復経済の確定。maxed実測10.2%で帯25〜40%未達（§18.2 v1.4/v1.8注記の正式起票。従来はOPEN表外だった） | 高 | Phase1前 | デザイナー決定→JSON反映（クラスB）→`gen_baseline`再生成→帯再測定。受入=Phase1開始可能 | 未着手 |
| OPEN-031 | サイドベット経済の検証統合 | RunRecord に賭け収支・的中数が無く（§19.1・gold_spent/action_counts/turn_history 列挙のいずれにも side_bet 無し・払戻の gold_earned 計上規則も未定義）、本採用条件だった「Phase4込み検証」（improvement_ideas ②⑤）が§18に未接続。オッズ3倍・cap40は仮値 | 中 | Phase4 | テレメトリ追加（wagered/hits/net または turn_history 導出を§19.2に明記）＋§18.1観測指標化＋informed/naive botでオッズ帯検証 | 未着手 |
| OPEN-032 | ランのライフサイクル | セッション復帰のフロントUX（session_id保存先・「続きから」導線・リロード復元）とリタイア/放棄の仕様・記録が未定義。放棄ランはRunRecord外＝生存バイアス・OPEN-029の離脱観測も不能（v1.3でOPEN-007に載ったDELETE論点の再拾い上げ） | 中 | 実装前半 | frontend_design へ復帰節＋放棄の記録方針（`abandoned` RunRecord 等）を定義。受入=リロードで進行中ランへ復帰できる＋放棄が統計に残る（or「観測対象外」と明記） | 未着手 |
| OPEN-033 | リスペック/プロファイルリセット | 恒久強化の振り直し可否・ニューゲーム（メタ進行初期化）導線が未定義（§12.3。DB手動削除以外に新規体験を再現できない） | 低 | 実装後半 | 不可なら設計意図を明文化、可ならコスト設計。OPEN-002（割り振り導線）と連動 | 未着手 |
| OPEN-034 | 型共有の自動化 | `api_schemas.py`⇄`types.ts` が手動同期（v1.4で tell_reliability ドリフトの前歴・§25.5は「将来可能」の言及止まり） | 中 | 実装後半 | OpenAPI→TS自動生成（openapi-typescript等）またはCI契約突合。受入=手書きtypes.tsの廃止 or CI検査導入 | 未着手 |
| OPEN-035 | 公開準備 | 年齢レーティング・「リアルマネー無し」明記・LICENSE・データ取り扱い宣言（§16.3の受け皿。旧OPEN-027参照は本IDへ付替え） | 低 | 公開前 | 公開チェックリスト化（README/ストア記載・LICENSE追加・収集データの1段落） | 未着手 |
| OPEN-036 | rebuild の版跨ぎ互換 | データ/エンジン変更を跨ぐ `active_sessions` 再生の互換性が未定義（バランスPRのデプロイで進行中ランが黙って変質/500し得る）。再構築コスト目安・LRU=500の根拠も未記載 | 中 | 実装後半 | `active_sessions` へ data_version 刻印＋不一致時の扱い（復元拒否→ラン破棄等）を仕様化。runbook へ「デプロイと進行中ラン」節 | 未着手 |
| OPEN-037 | pending契約の確定 | §20.8 の `empty_treasure`/`victory` の phase帰属・dismiss対応が「要確定」のまま（旧OPEN-013紐づけの宙吊り解消） | 低 | 実装後半 | 実装準拠で§20.8を確定し契約テストへ。受入=§20.8全行にphaseと解消操作が入る | 未着手 |
| OPEN-038 | 調書のカオス敵表示 | カオスはランごとに真の分布が変わる（§9.2）ため、ラン横断の観測集計＋Wilson CI（§15.3）が「収束するほど確信」の体験設計と噛み合わない。扱い未定義 | 低 | 実装後半 | CI非表示／「ランごとに変わる」注記／除外のいずれかをデザイナー決定。OPEN-028（カオス評価）と併せて処理可 | 未着手 |
| OPEN-039 | テルの開示ポリシー整合 | 気配（§15.5）は隠しweight由来の統計量を無料・常時開示するが、§5の非公開例外列挙に無く、3段階の判定式・閾値もどの文書にも無い | 低 | 本採用判断時 | §5へ「気配」行を追加し判定式（weight偏り帯等）をconfig定義。構成監査F-11のステータス確定（[実験的]明示）の受入条件に含める | 未着手 |
| OPEN-040 | 検死のheal死分類 | battle中heal（OPEN-020）で死亡した場合、検死3分類（§15.2＝攻撃/受け前提）が適用不能で契約テスト不能 | 低 | 実装後半 | 分類規則を追記（攻撃/受け両方の反実仮想を評価 or「回復が命取り」第4パターン）。実装準拠の追記で足りるならクラスD | 未着手 |
| OPEN-041 | L2アイコン解禁仕様 | §4.4末尾「L2アイコン解禁は初出フロアに合わせる」が孤立し、§5（L2=常時見える）と表面矛盾。未遭遇タイプのUI表示が未定義 | 低 | 実装後半 | §5のL2行へ解禁条件を統合し§4.4は参照化。NodeCardのフォールバック表示をfrontend_designへ | 未着手 |
| OPEN-042 | 強敵撃破数の記録 | 死亡画面の「強敵撃破数」（frontend_design §15.1）が RunRecord から導出不能（`enemies_defeated` に is_strong が無く現行enemies.json突合が必要＝data_version跨ぎ・OPEN-004定義変更で過去表示が変わる・フロント計算はADR-0002違反） | 低 | 実装後半 | `enemies_defeated` 要素へ撃破時点の `is_strong` を追加（Alembic・OPEN-025同型）。OPEN-004の定義方式と独立に過去記録を安定させる | 未着手 |
